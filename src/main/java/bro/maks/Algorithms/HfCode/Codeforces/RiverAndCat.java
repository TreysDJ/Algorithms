package bro.maks.Algorithms.HfCode.Codeforces;

import java.util.Scanner;

public class RiverAndCat {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int t = scanner.nextInt();
        for (int i = 0; i < t; i++) {
            int v = scanner.nextInt(); // ширина реки
            int xA = scanner.nextInt(); // координата начальной x
            int yA = scanner.nextInt(); // координата начальной y
            int xB = scanner.nextInt(); // координата конечной x
            int yb = scanner.nextInt(); // координата конечной y

        }
    }
}
